# Test Environment Configuration
# Copy this file to .env.test and update with your database settings

# Database Configuration
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB_TEST=trivia_game_test
POSTGRES_USER=postgres
POSTGRES_PASSWORD=

# Setup Instructions:
# 1. Copy this file: cp .env.test.example .env.test
# 2. Find your PostgreSQL username: psql -l
# 3. Update POSTGRES_USER above with your username
# 4. If prompted for password when connecting, add it to POSTGRES_PASSWORD
# 5. Run setup: cd src/__tests__ && ./setup-test-db.sh
# 6. Run tests: npm test

# Need help? Check src/__tests__/README.md for troubleshooting
