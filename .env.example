# Example environment configuration for PostgreSQL
# Copy this file to .env and update with your database credentials

# Database Configuration
# Choose your database type: 'supabase' or 'postgresql'
DATABASE_TYPE=postgresql

# PostgreSQL Configuration (when DATABASE_TYPE=postgresql)
# Option 1: Use individual connection parameters
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=trivia_game
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your_password_here

# Option 2: Use connection string (alternative to individual parameters)
# DATABASE_URL=postgresql://username:password@localhost:5432/trivia_game

# Supabase Configuration (when DATABASE_TYPE=supabase)
SUPABASE_URL=your-supabase-url
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# Server Configuration
PORT=3000
NODE_ENV=development
